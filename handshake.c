/*
  This server client relationship is known as the handshake
  to_server = handshake(&from_server);
  to_client = handshake(&from_client);

  *strchr(line,'\n') = 0; //replaces new line with the null 


*/
